<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.easthome.mapper.DressMapper">
    <select id="findAll" resultType="Dress">
        select * from dress
    </select>

    <select id="findByGoodsId" resultType="Dress">
        select * from dress where d_id = #{param1}
    </select>
</mapper>